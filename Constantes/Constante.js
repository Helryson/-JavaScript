/*
const PI = 3.14; // Sempre, ao definir uma constante, deixar as letras em maiúsculo.
let raio = Number(window.prompt("Insira o raio do círculo:"));
let circunferencia = 2 * raio * PI;
console.log(circunferencia);
*/

let z = 3.14;
let y = 6;
let x = -4;

let maximo;
let minimo;

// x = Math.round(x); // Se estiver na metade, irá arredondar para cima.
// x = Math.floor(x); //Arredonda para baixo, independente da casa decimal.
// x = Math.ceil(x); //Arredonda para cima.
// x = Math.pow(x, 2); // Elevação, 1° numero que vou elevar, 2° expoente.
// x = Math.sqrt(x); // Raiz quadrada.
// x = Math.abs(x); // Valor absoluto, módulo.

maximo = Math.max(x, y, z); // Ira pegar o maior valor entre os 3
minimo = Math.min(x, y, z);

// x = Math.PI; // x = pi com 15 casas decimais

console.log(max);

